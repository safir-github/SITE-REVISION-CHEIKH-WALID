// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  output: 'static',
  base: '/SITE-REVISION-CHEIKH-WALID',
  build: {
    format: 'directory'
  }
});
